for(var i=1;i<=ex_rec.getLineItemCount('item');i++)
			{
								
				if(ex_rec.getLineItemValue('item','custcol_removeitem',i) == 'T')
				{
					ex_rec.removeLineItem('item',i);
					i--;
				}
			} 
